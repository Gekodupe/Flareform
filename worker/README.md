# Worker moved

The Flareform API Worker now lives in the sibling package:

**`FlareformAPI/`** → [github.com/Gekodupe/FlareformAPI](https://github.com/Gekodupe/FlareformAPI)

```bash
cd ../../FlareformAPI
npm install
npm run deploy
```

This folder is kept only so older scripts do not break; do not edit Worker code here.
